filebeat.inputs:
- type: log
paths:
- '/var/lib/docker/containers/*/*.log'
json.message_key: log
json.keys_under_root: true
processors:
- add_docker_metadata: ~
  

output.logstash:
  hosts: ["127.0.0.1:5044"]
setup.kibana:
  host: "localhost:5601"

logging:
  level: debug
